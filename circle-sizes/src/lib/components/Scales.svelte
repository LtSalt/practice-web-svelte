<script context="module">
    import * as d3 from "d3";
    import Store from "$lib/store.js"

    const xScale = d3.scaleLinear()
        .domain([0, 1])
        .range([0, 200])

    // const yScale = d3.scaleBand()
    //     .domain([$Store.market.map()])

</script>